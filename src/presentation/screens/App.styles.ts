import { StyleSheet } from 'react-native';

export const SYSTEM_BAR_COLOR = '#304AA0';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: SYSTEM_BAR_COLOR,
  },
});
