<!--
  Prompt for GENERIC / SYSTEM icons that have no brand logo to convert
  (Settings, Dialer, Gallery, Files, etc.). Unlike config/icon-prompt.md, this is
  a pure text-to-image prompt: NO reference image is attached. Each app supplies a
  short {{subject}} (in config/icons.json) describing the symbol to draw; the disco
  art treatment below is shared so the whole system set stays visually consistent.
  Everything below the --- marker is sent to the image model verbatim.
-->
---
Create a premium disco-themed 3D app icon depicting: {{subject}}.
Render the symbol as a single, bold, instantly-recognizable launcher glyph — clean, simple, and modern, the kind of universally-understood pictogram a phone uses for this function. Keep the symbol's natural, conventional colors as the dominant palette; do NOT recolor into a rainbow or neon-wash.
Apply the disco treatment as reflective glossy 3D material, polished chrome edges, subtle sparkles, soft holographic accents and dynamic lighting that ENHANCE the symbol's colors — a clean premium look, not a literal disco ball.
NO SHADOWS (strict): fully transparent background (alpha = 0). Absolutely NO drop shadow, cast shadow, ground shadow, contact shadow, reflection puddle, or dark gradient beneath, behind, or around the icon. Every pixel outside the symbol silhouette must be fully transparent. Lighting and depth live ONLY on the object itself.
COMPOSITION (strict): the symbol must be perfectly centered in a square 1:1 frame with equal margins on all four sides. Upright and front-facing — no rotation, no tilt, no perspective skew, no off-center placement. The subject should fill about 80% of the frame, fully visible and never cropped. Keep ALL glow, sparkle, reflection and light effects contained inside the frame and symmetric around the center — no light rays or elements bleeding off the edges. A single, self-contained, balanced object.
Strong depth, modern launcher icon aesthetic, transparent background, optimized for Android.
